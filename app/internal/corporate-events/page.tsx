import { PlaceholderPage } from "@/components/internal/ui/PlaceholderPage";

export default function CorporateEventsPage() {
  return (
    <PlaceholderPage
      title="Azioni Societarie"
      subtitle="Fusioni, acquisizioni, scissioni, OPA e operazioni straordinarie"
      status="planned"
      description="Il modulo per le operazioni straordinarie e gli eventi societari rilevanti (M&A, OPA, scissioni) è in pianificazione per una fase successiva."
    />
  );
}
